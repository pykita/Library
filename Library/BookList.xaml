<Window x:Class="Library.BookList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="623">
    <Grid Margin="0,0,-8,0">
        <DataGrid x:Name="DgBooks" HorizontalAlignment="Left" Margin="10,10,0,0" 
                  VerticalAlignment="Top" Height="274" Width="458" 
                  ItemsSource="{Binding}" AutoGenerateColumns="False"
                  CanUserAddRows="True" CanUserDeleteRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" Width="40"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}"  Width="457"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnAddBook" Content="Add book" HorizontalAlignment="Left" Margin="505,36,0,0" VerticalAlignment="Top" Width="75" Click="btnAddBook_Click"/>
    </Grid>
</Window>
