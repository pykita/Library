<Window x:Class="Library.BookList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="623">
    <Grid Margin="0,0,-8,0">
        <DataGrid x:Name="DgBooks" HorizontalAlignment="Left" Margin="10,10,0,0" 
                  VerticalAlignment="Top" Height="274"  
                  ItemsSource="{Binding}" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn  IsReadOnly="True" Header="Id" Binding="{Binding Path=Id}" Width="20"/>
                <DataGridTextColumn IsReadOnly="True" Header="Author" Binding="{Binding Path=AuthorName}"  Width="70"/>
                <DataGridTextColumn IsReadOnly="True" Header="Name" Binding="{Binding Path=Name}"  Width="350"/>
                <DataGridTemplateColumn Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="EditBook_Click">Details</Button>
                        </DataTemplate>
                     </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnAddBook" Content="Add book" HorizontalAlignment="Left" Margin="523,33,0,0" VerticalAlignment="Top" Width="75" Click="btnAddBook_Click"/>
    </Grid>
</Window>
